name: 'atx_funding_rates'
version: '1.0.0'
config-version: 2

profile: 'atx_funding_rates'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  atx_funding_rates:
    staging:
      +materialized: view
    marts:
      +materialized: table
